//! Social features

/// Social system
pub struct SocialSystem;

impl SocialSystem {
    /// Create new social system
    pub fn new() -> Self {
        Self
    }
}

impl Default for SocialSystem {
    fn default() -> Self {
        Self::new()
    }
}
