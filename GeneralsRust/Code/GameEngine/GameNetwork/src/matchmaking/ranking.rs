//! Player ranking system

/// Ranking system
pub struct RankingSystem;

impl RankingSystem {
    /// Create new ranking system
    pub fn new() -> Self {
        Self
    }
}

impl Default for RankingSystem {
    fn default() -> Self {
        Self::new()
    }
}
