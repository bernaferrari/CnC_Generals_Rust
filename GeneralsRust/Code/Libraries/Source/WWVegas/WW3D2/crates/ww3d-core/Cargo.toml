[package]
name = "ww3d-core"
version = "0.1.0"
edition = "2021"

[dependencies]
glam = { version = "0.24", features = ["bytemuck"] }
thiserror = "1.0"
binrw = "0.11"
log = "0.4"
once_cell = "1.21"

[dev-dependencies]
