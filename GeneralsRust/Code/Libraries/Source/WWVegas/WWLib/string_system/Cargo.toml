[package]
name = "string_system"
version = "0.1.0"
edition = "2021"

[dependencies]
base_types = { path = "../base_types" }
memory_system = { path = "../memory_system" }
serde = { version = "1.0", features = ["derive"] }
parking_lot = "0.12"
libc = "0.2"

[features]
default = []
debug = []
unicode = []
