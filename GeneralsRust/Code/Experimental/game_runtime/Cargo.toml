[package]
name = "experimental-game-runtime"
version = "0.1.0"
edition = "2021"

[dependencies]
anyhow.workspace = true
pollster.workspace = true
wgpu.workspace = true
winit = "0.30"
experimental-engine-core = { path = "../engine_core", features = ["wgpu-backend"] }
