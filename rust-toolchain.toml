# Pin the workspace to a specific stable compiler. `stable` floats and
# produces irreproducible builds across machines; 1.98.1 is current stable
# (2026-09-01). Bump this when you deliberately take a new rustc.
[toolchain]
channel = "1.98.1"
components = ["rustfmt", "clippy", "rust-src"]
profile = "default"
