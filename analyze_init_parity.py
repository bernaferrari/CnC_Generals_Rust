[[", content)\n    if not init_match:\n        return []\n\n    start = init_match.start()\n    # Find matching closing brace\n    depth = 0\n    in_function = False\n    end_pos = start\n    for i in range(start, len(content)):\n        if content[i] == '{", 'depth += 1\n            in_function = True\n        elif content[i] == \'}\':\n            depth -= 1\n            if in_function and depth == 0:\n                end_pos = i + 1\n                break\n\n    init_body = content[start:end_pos]\n    lines = init_body.split(\'\n\')\n\n    sequence = []\n    for i, line in enumerate(lines):\n        line = line.strip()\n        if not line:\n            continue\n\n        # Identify init calls\n        if \'initSubsystem(\' in line:\n            # Extract subsystem variable name\n            m = re.search(r\'initSubsystem((w+)\', line)\n            if m:\n                subsystem = m.group(1)\n                # Extract INI paths if any\n                ini_paths = re.findall(r\'"Data\\\\INI\\\\[^'], {'type': 'subsystem', 'name': 'subsystem', 'line': 'i+1', 'text': 'line[:120]', 'ini_paths': 'ini_paths'}, ['InitRandom', 'TheFileSystem = createFileSystem', 'TheNameKeyGenerator->init', 'TheCommandList->init', 'parseCommandLine', 'TheGameLODManager->init', 'updateTGAtoDDS', 'ini.load', 'xferCRC.close', 'TheSubsystemList->postProcessLoadAll', 'setFramesPerSecondLimit', 'TheAudio->setOn', 'TheMapCache->updateCache', 'initKindOfMasks', 'initDisabledMasks', 'initDamageTypeFlags', 'TheSubsystemList->resetAll', 'HideControlBar'], [", line)\n            func_name = func_match.group(1) if func_match else line[:80]\n            sequence.append({\n                'type': 'operation", 'name', 'func_name,\n                \'line\': i+1,\n                \'text\': line[:120]\n            })\n\n    return sequence\n\ndef check_rust_implementation():\n    "', 'Check which C++ operations have equivalents in Rust"', '\n    rust_files = {\n        \'game_engine\': \'GeneralsRust/Code/GameEngine/Common/src/common/game_engine.rs\',\n        \'subsystem_interface\': \'GeneralsRust/Code/GameEngine/Common/src/common/system/subsystem_interface.rs\',\n    }\n\n    rust_content = {}\n    for name, path in rust_files.items():\n        try:\n            with open(path, \'r\') as f:\n                rust_content[name] = f.read()\n        except FileNotFoundError:\n            rust_content[name] = "', 'return rust_content\n\ndef analyze_parity(cpp_sequence, rust_content):\n    "', 'Analyze parity between C++ and Rust initialization"', '\n    results = {\n        \'total_cpp_steps\': len(cpp_sequence),\n        \'missing_in_rust\': [],\n        \'architectural_differences\': [],\n        \'notes\': []\n    }\n\n    for step in cpp_sequence:\n        step_name = step[\'name\'].lower()\n        found = False\n\n        # Search for equivalent in Rust game_engine init\n        if step[\'type\'] == \'subsystem\':\n            # Check if subsystem variable exists in Rust\n            rust_var = step[\'name\']\n            result = subprocess.run(\n                [\'grep\', \'-r\', f\'\\b{rust_var}\\b\', \'GeneralsRust/Code/GameEngine/Common/src\', \'--include=*.rs\'],\n                capture_output=True, text=True\n            )\n            if result.returncode == 0:\n                found = True\n            else:\n                # Check alternative naming (uppercase)\n                rust_var_alt = rust_var.upper()\n                result2 = subprocess.run(\n                    [\'grep\', \'-r\', f\'\\b{rust_var_alt}\\b\', \'GeneralsRust/Code/GameEngine/Common/src\', \'--include=*.rs\'],\n                    capture_output=True, text=True\n                )\n                if result2.returncode == 0:\n                    found = True\n                else:\n                    # Might be in SubsystemManager registry\n                    if \'SubsystemManager\' in rust_content.get(\'game_engine\', \'\'):\n                        found = True  # Registered via manager\n                        results[\'architectural_differences\'].append({\n                            \'step\': step[\'name\'],\n                            \'cpp\': step[\'text\'],\n                            \'reason\': \'Rust uses SubsystemManager registration instead of global var\'\n                        })\n\n        else:  # operation\n            # Map known operations to Rust equivalents\n            operation_map = {\n                \'InitRandom()\': [\'init_random\', \'InitRandom\', \'init_random_with_seed\'],\n                \'TheNameKeyGenerator->init()\': [\'NameKeyGenerator::init\'],\n                \'TheCommandList->init()\': [\'CommandList::init\'],\n                \'parseCommandLine(argc, argv)\': [\'parse_command_line\'],\n                \'TheGameLODManager->init()\': [\'GameLODManager::init\', \'init_game_lod\'],\n                \'xferCRC.close()\': [\'xfer_crc.close\', \'close_xfer_crc\'],\n                \'TheSubsystemList->postProcessLoadAll()\': [\'post_process_load_all\', \'postprocess_load_all\'],\n                \'setFramesPerSecondLimit(...)\': [\'set_frames_per_second_limit\', \'set_frame_rate\'],\n                \'TheSubsystemList->resetAll()\': [\'reset_all\'],\n                \'HideControlBar()\': [\'hide_control_bar\', \'hide_control_bar\'],\n                \'initKindOfMasks()\': [\'init_kind_of_masks\', \'init_kind_masks\'],\n                \'initDisabledMasks()\': [\'init_disabled_masks\', \'init_disabled_masks\'],\n                \'initDamageTypeFlags()\': [\'init_damage_type_flags\', \'init_damage_flags\'],\n                \'TheMapCache->updateCache()\': [\'MapCache::update_cache\', \'update_map_cache\'],\n                \'TheAudio->setOn(...)\': [\'audio.set_on\', \'Audio::set_on\'],\n            }\n\n            matched = False\n            for cpp_op, rust_patterns in operation_map.items():\n                if step_name.replace(\' \', \'\').startswith(cpp_op.split(\'(\')[0].replace(\' \', \'\')):\n                    for pattern in rust_patterns:\n                        if pattern.lower() in rust_content.get(\'game_engine\', \'\').lower():\n                            found = True\n                            matched = True\n                            break\n                    if not matched:\n                        # Might be in a different module\n                        results[\'missing_in_rust\'].append({\n                            \'step\': step[\'name\'],\n                            \'cpp\': step[\'text\'],\n                            \'line\': step[\'line\'],\n                            \'reason\': \'Operation not found in Rust init\'\n                        })\n                    break\n\n        if not found:\n            results[\'missing_in_rust\'].append({\n                \'step\': step[\'name\'],\n                \'cpp\': step[\'text\'],\n                \'line\': step[\'line\'],\n                \'type\': step[\'type\']\n            })\n\n    return results\n\ndef main():\n    print("=" * 80)\n    print("INITIALIZATION PARITY ANALYSIS")\n    print("=" * 80)\n\n    # Extract C++ sequence\n    cpp_sequence = extract_cpp_init_sequence()\n    print(f"C++ init steps found: {len(cpp_sequence)}', 'Load Rust content\n    rust_content = check_rust_implementation()\n\n    # Analyze parity\n    results = analyze_parity(cpp_sequence, rust_content)\n\n    print(f', 'nTotal C++ steps: {results[\'total_cpp_steps\']}")\n    print(f', 'Missing in Rust: {len(results[\'missing_in_rust\'])}")\n    print(f', "Architectural differences: {len(results['architectural_differences'])}", 'if results[\'missing_in_rust\']:\n        print("\n" + "=" * 80)\n        print("MISSING BEHAVIORS (needs patches)")\n        print("=', 80, "for item in results['missing_in_rust'][:20]:  # Show first 20\n            print(f", 'n{item[\'step\']} (line {item.get(\'line\', \'?\')})")\n            print(f', 'C++: {item[\'cpp\']}")\n            print(f', "Reason: {item.get('reason', 'Not found')}", 'if results[\'architectural_differences\']:\n        print("\n" + "=" * 80)\n        print("ARCHITECTURAL DIFFERENCES (acceptable if behavior matches)")\n        print("=', 80, "for item in results['architectural_differences'][:10]:\n            print(f", 'n{item[\'step\']}")\n            print(f', 'C++: {item[\'cpp\']}")\n            print(f', "Reason: {item['reason']}", 'Save full results\n    with open(\'init_parity_analysis.json\', \'w\') as f:\n        json.dump({\n            \'cpp_sequence\': cpp_sequence,\n            \'results\': results\n        }, f, indent=2)\n\n    print("\n" + "=" * 80)\n    print("Full analysis saved to init_parity_analysis.json")\n    print("=', 80, "if __name__ == '__main__':\n    main()"]]