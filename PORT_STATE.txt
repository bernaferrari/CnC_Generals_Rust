State updated: 2026-03-11 (menu parity batch 195)

Completed in this batch
- Ported C++ border remainder/overlap logic for:
  - `W3DMainMenuMapBorder` (`GameClient/src/gui/w3d_gadget_draw.rs`)
  - `drawSkinnyBorder` used by `W3DDrawMapPreview` (`GameClient/src/gui/callbacks/skirmish_map_select_menu.rs`)
- Added a compatibility `W3DNoDraw` bridge:
  - `W3DNoDraw` now draws through default image/video path when the window is image-backed, instead of hard no-op.
  - Wired in both:
    - `GameClient/src/gui/window_manager.rs`
    - `GameEngineDevice/src/w3_d_device/common/system/w3_d_function_lexicon.rs`

Why this matters
- Removes menu/frame edge artifacts caused by simplified border tiling math (half-piece and overlap adjustment cases).
- Reduces black/empty menu windows caused by Rust callback dispatch treating `W3DNoDraw` as unconditional no-op.

Validation executed
- `RUSTFLAGS='-Awarnings' cargo check -q -p game-client-rust -p game_engine_device`
- `RUSTFLAGS='-Awarnings' cargo run -q -p generals_main --bin playability_audit`
  - Result: `High-impact unresolved blockers: 0`

Known high-value remaining parity gaps
- Control bar callback parity:
  - `W3DCommandBarBackgroundDraw` / `W3DCommandBarForegroundDraw` still use fallback draw path instead of full C++ marker-offset scheme-manager rendering.
- Window callback coverage:
  - Some `.wnd` callback names still route to generic defaults (not full behavior), notably:
    - layout callbacks: `ChallengeMenu*`, `ReplayMenu*`, `MarketingScreen*`, `DifficultySelectInit`
    - system/input callbacks: `ChallengeMenu*`, `DifficultySelect*`, `ReplayMenu*`, plus minor callback aliases (`MOTDSystem`, `BeaconWindowInput`, `GadgetPushButton*`)
- Continue strict C++->Rust callback-by-callback closure in menu/campaign/replay screens.
